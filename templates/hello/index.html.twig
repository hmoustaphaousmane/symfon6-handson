{% extends 'base.html.twig' %}

{% block body %}
<div>
    {% if messages|length > 0 %}
    {% for message in messages %}
        <div>{{ message }}</div>
    {% endfor %}
    {% else %}
        There is nothing here yet.
    {% endif %}
</div>
{% endblock %}

{% block title %}I'va changed the title!{% endblock %}