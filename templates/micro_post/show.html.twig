{% extends "base.html.twig" %}

{% block title %}Post - {{ post.title }}{% endblock %}

{% block bread %}
→ Post
{% endblock %}

{% block body %}
    {{ include("micro_post/_post.html.twig", { post: post }) }}

    <div class="mt-5">
        {% for comment in post.ccomments %}
            <div class="">
                <div class="flex items-center">
                    <div class="inline-blick bg-gray-200 dark:bg-gray-700 rounded-full text-3xl w-8 h-8 ring-2 ring-white dark:ring-gray-600 mr-2"></div>
                    <div>
                        <div class="font-mediun text-gray-800 dark:text-gray-300">{{ comment.author.email }}</div>
                        <div class="font-ligth text-gray-500 dark:text-gray-500">{{ comment.created | date('M d, Y') }}</div>
                    </div>
                </div>
                <div class="flex">
                    <div class="border border-gray-300 dark:border-gray-600 ml-4 mr-6 pb-12">
                    </div>
                    <div>{{ comment.text }}</div>
                </div>
            </div>
        {% endfor %}
    </div>
{% endblock %}
